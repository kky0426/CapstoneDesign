<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capstona_a">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Capstone"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Capstona_a"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".IngameRateActivity"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog"
            android:exported="true" />
        <activity
            android:name=".BoardActivity"
            android:exported="true" />
        <activity android:name=".UserPhaseDetailActivity" android:theme="@style/Theme.Capstona_a" />
        <activity android:name=".IngameActivity" android:theme="@style/Theme.Capstona_a" />
        <activity android:name=".UserPhaseActivity" android:theme="@style/Theme.Capstona_a" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>